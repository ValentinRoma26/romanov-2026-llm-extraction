# Copy this file to `.env` and fill in your credentials.
# `.env` is git-ignored — NEVER commit real keys.

# --- LLM provider API keys (keep only the ones you use) ---
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- Optional ---
# HF_TOKEN=
