# Pin every model configuration used in the paper so runs are reproducible.
# Record the exact model id, provider, and the date the runs were executed
# (model behaviour can change between snapshots).

defaults:
  temperature: 0.0
  max_tokens: 4096
  seed: null

models:
  # - id: claude-...            # exact, dated model identifier
  #   provider: anthropic
  #   run_date: "2026-01-01"
  #   temperature: 0.0
  # - id: gpt-...
  #   provider: openai
  #   run_date: "2026-01-01"
